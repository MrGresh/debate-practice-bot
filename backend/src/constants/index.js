const { EMAIL_CONFIG } = require("./email.constants");
const { SERVER_CONFIG } = require("./server.constants");
const { AUTH_CONFIG } = require("./auth.constants");
const { SCHEDULER_CONFIG } = require("./scheduler.constants");
const { VAPI_CONFIG } = require("./vapi.constants");

module.exports = {
  EMAIL_CONFIG,
  SERVER_CONFIG,
  AUTH_CONFIG,
  SCHEDULER_CONFIG,
  VAPI_CONFIG,
};